# Left blank in dev so Vite's proxy handles it (keeps the refresh cookie same-origin).
VITE_API_URL=
VITE_TURNSTILE_SITE_KEY=
# WEB (default) or DESKTOP for a desktop-app build — decides how long a sign-in lasts.
VITE_SESSION_CLIENT=
